import styled from 'styled-components';
import { HomeContainer } from '../../../clientPanel/pages/Home/styles';

export const Error404Container = styled(HomeContainer)` 
    h2 {
        color: var(--primary-color);
    }
` ;